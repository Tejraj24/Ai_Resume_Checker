{
  "name": "ai-resume-checker-proxy",
  "version": "0.1.0",
  "description": "Minimal proxy to forward client requests to the analysis API (keeps API key server-side)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^3.4.1"
  }
}
